<h1>Edit</h1>

<%= form_for(@user) do |f| %>

  <%= render 'basic_user_fields', :f => f %>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<div>
  <%= gravatar_for @user %>
  <a href="http://gravatar.com/emails">change</a>
</div>