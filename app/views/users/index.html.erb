<div class="column span-16 main">
<h1>Users</h1>

<%-# Will_paginate method automatically adds :page to params and handles pagination%>
<%= will_paginate %>

<ul class="users" id="paged_content">

  <%= render @users %>

</ul>

<%= will_paginate %>
<%= pageless(@users.total_pages, request.path) %>
</div>